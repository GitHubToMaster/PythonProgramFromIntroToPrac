import matplotlib.pyplot as plt
from random import choice


class

while True:
    x_values.append(x_distance)
    y_values.append(y_values)



    # 拒绝原地踏步
    if x_distance == 0 and y_distance == 0:
        x_distance +=
    x_position = x_step * x_distance
    y_position = y_step * y_distance





    plt.show()